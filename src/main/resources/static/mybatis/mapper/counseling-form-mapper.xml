<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jcCounselingForm">

	<insert id="insertCounselingForm"
		parameterType="com.team_3.dto.CounselingFormDTO">
		INSERT INTO CounselingForm (date, time, NAME,
		studentNumber, selectedType, email, counselingContent)
		VALUES (#{date},
		#{time}, #{NAME}, #{studentNumber}, #{selectedType}, #{email},
		#{counselingContent})
	</insert>

	<select id="findByStud_no" resultType="String">
		SELECT STUD_NO FROM student_user_view WHERE NAME = #{name};
	</select>

	<select id="selectGroupData" resultType="BoardDTO">
		SELECT * FROM JDSD_Info
	</select>

	<select id="getDetail" resultType="BoardDTO">
		SELECT JD_NO, PG_NAME,
		PG_EXPLAIN, EN_DATE, JMS_CS_DATE, TO_NO, AP_NO, PA_NO
		FROM JDSD_Info
		WHERE JD_NO = #{no}
		and EN_NO = "2"
	</select>

	<select id="getSchedule" resultType="HashMap">
		select * from CODE
		where
		`DIV` = 'SC01'
	</select>

	<!-- 지도교수 상담 테이블명 수정해야함-->
	<insert id="saveAdvisorCounselingForm" parameterType="com.team_3.dto.CounselingFormDTO">
		INSERT INTO advisor_ (JMS_NO, ST_NO, JMS_RS_DATE,
		JMS_RS_TIME, JMS_CS_DATE, JMS_CS_TIME, JMS_RS_TEXT, RSVT_YN, JMS_LIST,
		SC_SN)
		VALUES (#{JMS_NO}, #{ST_NO}, #{JMS_RS_DATE}, #{JMS_RS_TIME}, #{JMS_CS_DATE},
		#{JMS_CS_TIME}, #{JMS_RS_TEXT}, #{RSVT_YN}, #{JMS_LIST}, #{SC_SN})
	</insert>

	<!-- 전문 상담 테이블명 수정해야함-->
	<insert id="saveExpertCounselingForm"	parameterType="com.team_3.dto.CounselingFormDTO">
		INSERT INTO expert_counseling_forms (JMS_NO, JMS_BF, ST_NO, JMS_RS_DATE,
		JMS_RS_TIME, JMS_CS_DATE, JMS_CS_TIME, JMS_RS_TEXT, RSVT_YN, JMS_LIST,
		SC_SN)
		VALUES (#{JMS_NO}, #{JMS_BF}, #{ST_NO}, #{JMS_RS_DATE}, #{JMS_RS_TIME},
		#{JMS_CS_DATE}, #{JMS_CS_TIME}, #{JMS_RS_TEXT}, #{RSVT_YN},
		#{JMS_LIST}, #{SC_SN})
	</insert>

	<!-- 심리 상담 테이블명 수정해야함 -->
	<insert id="savePsychologicalCounselingForm" parameterType="com.team_3.dto.CounselingFormDTO">
		INSERT INTO psychological_counseling_forms (JMS_NO, ST_NO, JMS_RS_DATE,
		JMS_RS_TIME, JMS_CS_DATE, JMS_CS_TIME, JMS_RS_TEXT, RSVT_YN, JMS_LIST,
		SC_SN)
		VALUES (#{JMS_NO}, #{ST_NO}, #{JMS_RS_DATE}, #{JMS_RS_TIME}, #{JMS_CS_DATE},
		#{JMS_CS_TIME}, #{JMS_RS_TEXT}, #{RSVT_YN}, #{JMS_LIST}, #{SC_SN})
	</insert>

	<!-- 취업 상담 양식-->
	<insert id="saveEmploymentCounselingForm" parameterType="com.team_3.dto.CounselingFormDTO">
    	INSERT INTO JC_LIST (RS_DATE, RS_TIME, NAME, STUD_NO, email, CS_TEXT )
    	VALUES (#{RS_DATE}, #{RS_TIME}, #{NAME}, #{STUD_NO}, #{email}, #{CS_TEXT})
	</insert>


</mapper>